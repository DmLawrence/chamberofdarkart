import React from 'react';
import Pic from './animated-michael-myers.png';

const Gallery = () => {
	return (
		<div>
			<Canvas title={animated-michael-myers} art={Pic} />
		</div>
	);
}

export default Gallery;